# ─────────────────────────────────────────────────────────
# NEUTRAL STARSHIP PROMPT CONFIGURATION
# Greyscale-first • Subtle accents • Universally compatible
# Designed to blend with any terminal colorscheme
# ─────────────────────────────────────────────────────────

"$schema" = 'https://starship.rs/config-schema.json'

# ▓▓▓ GLOBAL CONFIGURATION ▓▓▓
add_newline = false
command_timeout = 1000
scan_timeout = 30

# ▓▓▓ PROMPT FORMAT ▓▓▓
format = """
$fill\
$time\
$line_break\
$all\
$line_break\
$character"""

right_format = """
$status\
$cmd_duration"""

# ▓▓▓ FILL CHARACTER ▓▓▓
[fill]
symbol = '─'
style = 'dimmed white'

# ▓▓▓ SYSTEM TIME ▓▓▓
[time]
disabled = false
format = '[$time]($style)'
style = 'bold white'
time_format = '%H:%M:%S'
utc_time_offset = 'local'

# ─────────────────────────────────────────────────────────
# COMMAND INTERFACE
# ─────────────────────────────────────────────────────────

[character]
format = '''
$symbol '''
success_symbol = '[❯](bold white)'
error_symbol = '[❯](bold red)'
vimcmd_replace_one_symbol = '[❮](bold yellow)'
vimcmd_replace_symbol = '[❮](bold yellow)'
vimcmd_visual_symbol = '[❮](bold yellow)'

# ▓▓▓ USER ▓▓▓
[username]
style_user = 'bold white'
style_root = 'bold red'
format = '[$user]($style)[@](dimmed white)'
disabled = false
show_always = true

# ▓▓▓ HOSTNAME ▓▓▓
[hostname]
ssh_only = false
format = '[$hostname]($style)[∿](dimmed white)'
style = 'bold white'
disabled = false

# ▓▓▓ DIRECTORY ▓▓▓
[directory]
style = 'bold white'
format = ' [$path]($style)[$read_only]($read_only_style)'
truncation_length = 3
truncate_to_repo = false
truncation_symbol = '…/'
read_only = '[🔒]'
read_only_style = 'bold red'
home_symbol = '~'
use_os_path_sep = true

# ─────────────────────────────────────────────────────────
# VERSION CONTROL
# ─────────────────────────────────────────────────────────

[git_branch]
style = 'bold white'
format = ' [$symbol$branch(:$remote_branch)]($style) '
symbol = ' '
truncation_length = 25
truncation_symbol = '…'
only_attached = false
always_show_remote = false

[git_commit]
style = 'bold white'
format = '[◉$hash$tag]($style)'
commit_hash_length = 7
tag_symbol = ' 🏷 '
tag_disabled = false
only_detached = true

# ─────────────────────────────────────────────────────────
# SYSTEM DIAGNOSTICS
# ─────────────────────────────────────────────────────────

[cmd_duration]
style = 'bold yellow'
min_time = 500
format = '[⏱ $duration]($style)'
show_milliseconds = true
show_notifications = false
min_time_to_notify = 45000

[status]
style = 'bold red'
symbol = '✘ '
success_symbol = ''
not_executable_symbol = '🚫'
not_found_symbol = '❓'
sigint_symbol = '🛑'
signal_symbol = '⚡'
format = '[$symbol$status]($style)'
map_symbol = false
disabled = false
pipestatus = true
pipestatus_separator = '|'

[jobs]
style = 'bold yellow'
symbol = '⚙ '
number_threshold = 1
symbol_threshold = 1
format = '[$symbol$number]($style)'

# ─────────────────────────────────────────────────────────
# DEVELOPMENT ENVIRONMENT
# ─────────────────────────────────────────────────────────

[nodejs]
style = 'bold white'
symbol = ' '
format = '[$symbol($version)]($style)'
version_format = 'v${raw}'
detect_extensions = ['js', 'mjs', 'cjs', 'ts', 'tsx']
detect_files = ['package.json', '.node-version', '.nvmrc']
detect_folders = ['node_modules']

[python]
style = 'bold white'
symbol = ' '
format = '[${symbol}${pyenv_prefix}(${version})(\($virtualenv\))]($style)'
version_format = 'v${raw}'
detect_extensions = ['py']
detect_files = [
    'Pipfile',
    '__init__.py',
    'pyproject.toml',
    'requirements.txt',
    'setup.py',
    'tox.ini',
]
pyenv_version_name = false
pyenv_prefix = 'pyenv '

[rust]
style = 'bold white'
symbol = ' '
format = '[$symbol($version)]($style)'
version_format = 'v${raw}'
detect_extensions = ['rs']
detect_files = ['Cargo.toml']

[golang]
style = 'bold white'
symbol = ' '
format = '[$symbol($version)]($style)'
version_format = 'v${raw}'
detect_extensions = ['go']
detect_files = [
    'go.mod',
    'go.sum',
]
detect_folders = []

[docker_context]
style = 'bold white'
symbol = ' '
format = '[$symbol$context]($style)'
only_with_files = true
detect_extensions = []
detect_files = ['docker-compose.yml', 'docker-compose.yaml', 'Dockerfile']
detect_folders = []

[kubernetes]
style = 'bold white'
symbol = '☸ '
format = '[$symbol$context( \($namespace\))]($style)'
disabled = false
detect_extensions = []
detect_files = []
detect_folders = []

[battery]
format = '[$symbol$percentage]($style)'
[[battery.display]]
threshold = 20
style = 'bold red'
[[battery.display]]
threshold = 50
style = 'bold yellow'
[[battery.display]]
threshold = 100
style = 'bold white'

# ─────────────────────────────────────────────────────────
[line_break]
disabled = false
# ─────────────────────────────────────────────────────────
# END — neutral prompt, ready for any colorscheme
# ─────────────────────────────────────────────────────────
